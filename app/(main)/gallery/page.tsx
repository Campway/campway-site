import { GalleryClient } from './gallery-client';

export const metadata = {
  title: 'Gallery | Campway Customs',
  description: 'Browse our portfolio of completed Toyota HiAce van conversions, interior fitouts, and custom builds.',
};

export default function GalleryPage() {
  return <GalleryClient />;
}
